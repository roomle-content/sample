{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Rubens CLI",
            "type": "node",
            "request": "launch",
            "cwd": "${workspaceRoot}",
            "runtimeExecutable": "npm",
            "runtimeArgs": [
                "run", 
                "dev"
            ]
        }
    ]
}
